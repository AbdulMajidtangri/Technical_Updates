/**
 * Feature flags — flip these when ready to launch.
 * Newsletter code lives in lib/newsletter/, components/newsletter/, app/api/newsletter/
 */
export const FEATURES = {
  /** Set true after domain + SMTP email are configured */
  newsletter: false,
};

export default FEATURES;
